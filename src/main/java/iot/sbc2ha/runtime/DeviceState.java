package iot.sbc2ha.runtime;

/**
 * Stable state of a togglable output device (light or output pin).
 */
public enum DeviceState {
    /** Device is on / activated. */
    ON,
    /** Device is off / deactivated. */
    OFF
}
