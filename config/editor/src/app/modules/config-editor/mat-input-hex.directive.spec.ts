import { MatInputHexDirective } from './mat-input-hex.directive';

describe('MatInputHexDirective', () => {
  it('should create an instance', () => {
    const directive = new MatInputHexDirective();
    expect(directive).toBeTruthy();
  });
});
